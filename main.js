function saveHomeData() {
    let dataFrom = document.getElementById('from').value
    let dataTo = document.getElementById('to').value
    let dataMessage = document.getElementById('message').value
}